这是 `vchat` 的开发模板，更多内容请查看 [vchat 主页](https://github.com/tmkook/vchat) 

你可以在 `main.js` 文件中调用 `vchat` 自动化方法。编写完成后执行  `npm run build` 后将 `dist/main.js` 文件运行到 `hamibot` 即可。

编译最终执行文件

```
npm run build
```